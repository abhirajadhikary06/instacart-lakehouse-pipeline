# =============================================================================
# Root requirements.txt — local development (Phase 1)
# Install into your venv:  pip install -r requirements.txt
#
# Container-specific deps live in:
#   spark/requirements.txt     — installed into the Spark image
#   airflow/requirements.txt   — installed into the Airflow image (Phase 2+)
# =============================================================================

# --- Kaggle dataset acquisition -------------------------------------------
kaggle==1.6.17

# --- dlt (data load tool) — filesystem + parquet extras -------------------
# filesystem destination  = writes to S3 / MinIO
# parquet extra           = pyarrow for Parquet output
dlt[filesystem,parquet]==1.4.1

# --- Object storage client ------------------------------------------------
boto3==1.34.144

# --- Data processing (local validation / scripts) -------------------------
pandas==2.2.2
pyarrow==16.1.0

# --- Environment config ---------------------------------------------------
python-dotenv==1.0.1

# --- Utilities ------------------------------------------------------------
pyyaml==6.0.1
requests==2.32.3

# --- Testing --------------------------------------------------------------
pytest==8.3.2
pytest-cov==5.0.0

# --- Linting / formatting -------------------------------------------------
ruff==0.5.2
black==24.4.2

# --- Phase 2+ (uncomment when reaching those phases) ----------------------
# apache-airflow==2.9.2
# dbt-core==1.8.2
# dbt-postgres==1.8.2
# great_expectations==0.18.19
