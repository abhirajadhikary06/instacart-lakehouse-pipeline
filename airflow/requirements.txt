apache-airflow-providers-apache-spark==4.9.0
apache-airflow-providers-amazon==8.27.0
apache-airflow-providers-postgres==5.12.0
apache-airflow-providers-common-sql==1.14.2
kaggle==1.6.17
boto3==1.34.144
# dbt-postgres==1.8.2        # Phase 3 — add when dbt tasks are needed
# dlt[filesystem,parquet]    # Not needed inside Airflow — dlt runs locally
